import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liverpoolfc.news',
  appName: 'Liverpool FC News',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
